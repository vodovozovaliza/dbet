{
  "name": "dbet-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "DBET Live monorepo",
  "scripts": {
    "build": "npm install --prefix server && npm run prisma:generate --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev": "npm run dev --prefix server"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
